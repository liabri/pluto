# HTTP to HTTPS redirection
#http:// {
#	redir https://{host}{uri} permanent
#}

:80 {
	handle_path /weblog* {
		reverse_proxy http://weblog:8080
	}

#	handle_path /dev*
#	handle_path /git*
#	handle_path /docs*

	reverse_proxy http://photography:80
}

:443 {
	tls internal
	respond "BOO!"
}

